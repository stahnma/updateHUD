Reconnect if server comes back
Debug output in too  many spots
IP can't be hard-coded.
New systems are not auto showing up
SystemDunits
Flox setup
Clean up git history.
rename project
configure actions
source for debian repos shows up as "unknown", but the package name has the source in it.
